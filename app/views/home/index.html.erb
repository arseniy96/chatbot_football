<%= form_for :chant, url: chants_path do |f| %>
  <%= f.label :user_id, 'Введите id ВК' %>
  <%= f.text_field :user_id %>

  <%= f.submit %>
<% end %>

<%= current_user.firstname if user_signed_in? %>